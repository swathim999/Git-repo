<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.multi.language">

    <application
        android:name="io.flutter.app.FlutterApplication"
        android:label="multilingual_call_app"
        android:icon="@mipmap/ic_launcher">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|screenLayout|fontScale|screenDensity|navigation|rotation|smallestScreenSize"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Meta-data for Flutter V2 embedding -->
            <meta-data
                android:name="io.flutter.embedding.android.MainActivity"
                android:value="true" />
        </activity>

    </application>
</manifest>
