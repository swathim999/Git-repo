name: multilingual_call_app
description: A new Flutter project for multilingual calling.
version: 1.0.0+1
environment:
  sdk: "^3.5.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0
  firebase_storage: ^12.3.6
  firebase_messaging: ^15.1.5
  flutter_tts: ^4.2.0
  speech_to_text: ^7.0.0
  agora_rtc_engine: ^6.3.2

